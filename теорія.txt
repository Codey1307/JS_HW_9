Опишіть, як можна створити новий HTML тег на сторінці.
- за допомогою document.createElement 
Опишіть, що означає перший параметр функції insertAdjacentHTML і опишіть можливі варіанти цього параметра.
-  перший параметр визначає позицію елемента, що ми додаємо відносно елемента, до якого ми використовуємо метод. 
beforebegin(до елементу), 
afterbegin(одразу після відкриваючого тегу)
beforeend(одразу перед закриваючим тегом)
afterend(безпосереднбьо після елемента)
Як можна видалити елемент зі сторінки?
- за допомогою методів remove() або removeChild()